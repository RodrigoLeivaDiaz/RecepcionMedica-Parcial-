@using recepcionMedica.ViewModels;

@model EspecialidadesViewModel;

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Especialidad</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Especialidad)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Especialidad)
        </dd>
    </dl>
    <div>
            <h3>Lista de Medicos Especialitas en esta profesión</h3>
            <ol>
                @foreach (var item in Model.Medicos)
                {
                    <li>@item.NombreCompleto</li>
                }
            </ol>
        </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
